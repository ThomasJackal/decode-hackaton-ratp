{% extends 'base.html.twig' %}

{% block title %}Modifier le signalement{% endblock %}

{% block body %}
    <div class="page-intro">
        <h1 class="page-title">Modifier le signalement</h1>
    </div>

    <div class="form-panel">
        {{ include('report/_form.html.twig', { button_label: 'Mettre à jour' }) }}
        <div class="stack-actions">
            <a href="{{ path('app_report_show', {'id': report.id}) }}" class="btn btn--secondary">Annuler</a>
        </div>
    </div>
{% endblock %}
