# "submit" stays stateless (Turbo-friendly). "authenticate" uses the session so login works reliably.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            - logout
